# Sweep: lambda_penalty × seed for 10-partition torus
# Explores regularization strength with 2 random seeds per value.
# Strategy: grid → 6 × 2 = 12 runs

name: lambda-seed
base_config: parameters/torus_10part.yaml

sweep:
  relaxation.lambda_penalty: [3.1, 3.2, 3.3]
  relaxation.seed: [42359876]
  resolution:
    surface.torus.n_theta: [42, 54, 66, 78, 80]
    surface.torus.n_phi: [38, 50, 62, 74, 76]

strategy: grid
